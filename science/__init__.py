"""Authoritative scientific core.

All row selection, physical transformation, classification, uncertainty model,
survey selection, and estimator logic belongs below this package. It must not
depend on :mod:`plotting`; see the architecture boundary in ``README.md``.
"""
